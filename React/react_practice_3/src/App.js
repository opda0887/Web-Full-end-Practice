import React from "react";

// ? use arrow function expression to create a function element
const App = () => {
  return (
    <div>
      <h1>Hello</h1>
      <h3>This this the formal React practice.</h3>
      <button style={{ color: "red" }}>Click for more information</button>
    </div>
  );
};

// ? export App function element
export default App;
